    def animation_saury(self):
        return

    def animation_sardine(self):
        return

    def animation_squid(self):
        return

    def run_animate(self):
        for i in range(len(self.saury_text)):
            self.bonus_menu.after(200, self.animation_saury)
        for i in range(len(self.saury_text)):
            self.bonus_menu.after(200, self.animation_sardine)
        for i in range(len(self.saury_text)):
            self.bonus_menu.after(200, self.animation_squid)